<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Nexa CRM - All-in-one business solution for client management, marketing automation, and sales growth. Start your free trial today.">
    <meta name="keywords" content="CRM, client management, sales automation, marketing automation, business growth">
    <title>Nexa CRM | Simplify Your Business Growth</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --primary-blue: #2563eb;
            --dark-blue: #1e40af;
            --light-blue: #dbeafe;
            --accent: #3b82f6;
            --text-dark: #1e293b;
            --text-gray: #64748b;
            --bg-light: #f8fafc;
            --white: #ffffff;
        }

        html {
            scroll-behavior: smooth;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            line-height: 1.6;
            color: var(--text-dark);
            overflow-x: hidden;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }

        /* Navigation */
        nav {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            padding: 20px 0;
            position: fixed;
            width: 100%;
            top: 0;
            z-index: 1000;
            box-shadow: 0 2px 20px rgba(0,0,0,0.05);
            transition: all 0.3s ease;
        }

        nav.scrolled {
            padding: 15px 0;
            box-shadow: 0 2px 30px rgba(0,0,0,0.1);
        }

        .nav-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            font-size: 28px;
            font-weight: 700;
            background: linear-gradient(135deg, var(--primary-blue), var(--accent));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            cursor: pointer;
        }

        .nav-links {
            display: flex;
            gap: 30px;
            align-items: center;
        }

        .nav-links a {
            color: var(--text-dark);
            text-decoration: none;
            font-weight: 500;
            transition: color 0.3s;
            position: relative;
        }

        .nav-links a:not(.btn-nav):hover {
            color: var(--primary-blue);
        }

        .nav-links a:not(.btn-nav)::after {
            content: '';
            position: absolute;
            bottom: -5px;
            left: 0;
            width: 0;
            height: 2px;
            background: var(--primary-blue);
            transition: width 0.3s;
        }

        .nav-links a:not(.btn-nav):hover::after {
            width: 100%;
        }

        .btn-nav {
            background: var(--primary-blue);
            color: var(--white);
            padding: 10px 24px;
            border-radius: 8px;
            font-weight: 600;
            transition: all 0.3s;
        }

        .btn-nav:hover {
            background: var(--dark-blue);
            transform: translateY(-2px);
            box-shadow: 0 10px 20px rgba(37, 99, 235, 0.3);
        }

        .mobile-menu {
            display: none;
            flex-direction: column;
            gap: 5px;
            cursor: pointer;
        }

        .mobile-menu span {
            width: 25px;
            height: 3px;
            background: var(--text-dark);
            transition: all 0.3s;
        }

        /* Hero Section */
        .hero {
            margin-top: 80px;
            padding: 120px 0 100px;
            background: linear-gradient(135deg, #f0f9ff 0%, #e0f2fe 50%, #dbeafe 100%);
            position: relative;
            overflow: hidden;
        }

        .hero::before,
        .hero::after {
            content: '';
            position: absolute;
            border-radius: 50%;
            background: radial-gradient(circle, rgba(37, 99, 235, 0.08) 0%, transparent 70%);
        }

        .hero::before {
            width: 800px;
            height: 800px;
            top: -400px;
            right: -200px;
            animation: float 8s ease-in-out infinite;
        }

        .hero::after {
            width: 600px;
            height: 600px;
            bottom: -300px;
            left: -100px;
            animation: float 10s ease-in-out infinite reverse;
        }

        @keyframes float {
            0%, 100% { transform: translate(0, 0) rotate(0deg); }
            33% { transform: translate(30px, -30px) rotate(5deg); }
            66% { transform: translate(-20px, 20px) rotate(-5deg); }
        }

        .hero-content {
            text-align: center;
            position: relative;
            z-index: 1;
            max-width: 800px;
            margin: 0 auto;
        }

        .hero-content h1 {
            font-size: 56px;
            font-weight: 800;
            line-height: 1.1;
            margin-bottom: 24px;
            background: linear-gradient(135deg, var(--text-dark) 0%, var(--primary-blue) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            opacity: 0;
            animation: fadeInUp 0.8s ease-out forwards;
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .hero-content p {
            font-size: 22px;
            color: var(--text-gray);
            margin-bottom: 40px;
            opacity: 0;
            animation: fadeInUp 0.8s ease-out 0.2s forwards;
        }

        .hero-buttons {
            display: flex;
            gap: 20px;
            justify-content: center;
            flex-wrap: wrap;
            opacity: 0;
            animation: fadeInUp 0.8s ease-out 0.4s forwards;
        }

        .btn-primary {
            background: var(--primary-blue);
            color: var(--white);
            padding: 18px 40px;
            border-radius: 12px;
            text-decoration: none;
            font-weight: 600;
            font-size: 18px;
            transition: all 0.3s;
            border: none;
            cursor: pointer;
            display: inline-block;
        }

        .btn-primary:hover {
            background: var(--dark-blue);
            transform: translateY(-3px);
            box-shadow: 0 15px 30px rgba(37, 99, 235, 0.4);
        }

        .btn-secondary {
            background: var(--white);
            color: var(--primary-blue);
            padding: 18px 40px;
            border-radius: 12px;
            text-decoration: none;
            font-weight: 600;
            font-size: 18px;
            border: 2px solid var(--primary-blue);
            transition: all 0.3s;
            display: inline-block;
        }

        .btn-secondary:hover {
            background: var(--primary-blue);
            color: var(--white);
            transform: translateY(-3px);
            box-shadow: 0 15px 30px rgba(37, 99, 235, 0.3);
        }

        /* Section Styles */
        section {
            padding: 100px 0;
            position: relative;
        }

        .section-header {
            text-align: center;
            margin-bottom: 60px;
        }

        .section-header h2 {
            font-size: 44px;
            font-weight: 700;
            margin-bottom: 16px;
            color: var(--text-dark);
        }

        .section-header p {
            font-size: 18px;
            color: var(--text-gray);
            max-width: 600px;
            margin: 0 auto;
        }

        /* Features Section */
        .features {
            background: var(--white);
        }

        .features-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
            gap: 40px;
        }

        .feature-card {
            padding: 40px 30px;
            border-radius: 16px;
            background: var(--bg-light);
            transition: all 0.4s ease;
            text-align: center;
            border: 2px solid transparent;
        }

        .feature-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
            background: var(--white);
            border-color: var(--light-blue);
        }

        .feature-icon {
            width: 70px;
            height: 70px;
            background: linear-gradient(135deg, var(--primary-blue), var(--accent));
            border-radius: 16px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 24px;
            font-size: 32px;
            transition: all 0.4s;
        }

        .feature-card:hover .feature-icon {
            transform: rotateY(360deg);
        }

        .feature-card h3 {
            font-size: 22px;
            margin-bottom: 12px;
            color: var(--text-dark);
        }

        .feature-card p {
            color: var(--text-gray);
            line-height: 1.7;
        }

        /* Benefits Section */
        .benefits {
            background: linear-gradient(135deg, var(--bg-light) 0%, var(--white) 100%);
        }

        .benefits-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 40px;
            margin-top: 60px;
        }

        .benefit-card {
            text-align: center;
            padding: 50px 30px;
            background: var(--white);
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.05);
            transition: all 0.3s;
            position: relative;
            overflow: hidden;
        }

        .benefit-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 4px;
            background: linear-gradient(90deg, var(--primary-blue), var(--accent));
            transform: scaleX(0);
            transition: transform 0.3s;
        }

        .benefit-card:hover::before {
            transform: scaleX(1);
        }

        .benefit-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.12);
        }

        .benefit-icon {
            font-size: 48px;
            margin-bottom: 20px;
        }

        .benefit-card h3 {
            font-size: 24px;
            margin-bottom: 12px;
            color: var(--text-dark);
        }

        .benefit-card p {
            color: var(--text-gray);
            font-size: 16px;
        }

        /* Product Images Section */
        .product-images {
            background: var(--white);
        }

        .images-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: 40px;
            margin-top: 60px;
        }

        .image-card {
            background: var(--bg-light);
            border-radius: 20px;
            overflow: hidden;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.08);
            transition: all 0.3s;
        }

        .image-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 20px 50px rgba(0, 0, 0, 0.15);
        }

        .image-placeholder {
            width: 100%;
            height: 300px;
            background: linear-gradient(135deg, var(--light-blue) 0%, #e0e7ff 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 48px;
        }

        .image-caption {
            padding: 30px;
            background: var(--white);
        }

        .image-caption h3 {
            font-size: 20px;
            margin-bottom: 10px;
            color: var(--text-dark);
        }

        .image-caption p {
            color: var(--text-gray);
            line-height: 1.6;
        }

        /* Video Demo Section */
        .video-demo {
            background: linear-gradient(135deg, #f0f9ff 0%, #e0f2fe 100%);
        }

        .video-container {
            max-width: 900px;
            margin: 60px auto 0;
            border-radius: 20px;
            overflow: hidden;
            box-shadow: 0 30px 60px rgba(0, 0, 0, 0.2);
            transition: all 0.3s;
        }

        .video-container:hover {
            transform: translateY(-10px);
            box-shadow: 0 40px 70px rgba(0, 0, 0, 0.25);
        }

        .video-placeholder {
            width: 100%;
            aspect-ratio: 16/9;
            background: linear-gradient(135deg, var(--dark-blue) 0%, var(--primary-blue) 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--white);
            font-size: 80px;
            cursor: pointer;
            position: relative;
        }

        .play-button {
            width: 100px;
            height: 100px;
            background: rgba(255, 255, 255, 0.9);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--primary-blue);
            font-size: 40px;
            transition: all 0.3s;
        }

        .video-placeholder:hover .play-button {
            transform: scale(1.1);
            background: var(--white);
        }

        /* Pricing Section */
        .pricing {
            background: var(--white);
        }

        .pricing-container {
            max-width: 600px;
            margin: 60px auto 0;
        }

        .pricing-card {
            background: var(--white);
            border-radius: 24px;
            padding: 50px;
            box-shadow: 0 30px 60px rgba(0, 0, 0, 0.15);
            position: relative;
            overflow: hidden;
            transition: all 0.3s;
        }

        .pricing-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 6px;
            background: linear-gradient(90deg, var(--primary-blue), var(--accent));
        }

        .pricing-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 40px 70px rgba(0, 0, 0, 0.2);
        }

        .pricing-header {
            text-align: center;
            margin-bottom: 40px;
            padding-bottom: 30px;
            border-bottom: 2px solid var(--bg-light);
        }

        .pricing-header h3 {
            font-size: 32px;
            margin-bottom: 16px;
            color: var(--text-dark);
        }

        .pricing-price {
            font-size: 64px;
            font-weight: 800;
            color: var(--primary-blue);
            margin-bottom: 8px;
        }

        .pricing-price span {
            font-size: 28px;
            color: var(--text-gray);
            font-weight: 400;
        }

        .pricing-features {
            list-style: none;
            margin-bottom: 40px;
        }

        .pricing-features li {
            padding: 14px 0;
            color: var(--text-dark);
            display: flex;
            align-items: center;
            gap: 12px;
            font-size: 17px;
        }

        .pricing-features li::before {
            content: '✓';
            color: var(--primary-blue);
            font-weight: 700;
            font-size: 22px;
            flex-shrink: 0;
        }

        /* Testimonials Section */
        .testimonials {
            background: linear-gradient(135deg, var(--bg-light) 0%, var(--white) 100%);
        }

        .testimonials-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
            gap: 40px;
            margin-top: 60px;
        }

        .testimonial-card {
            background: var(--white);
            padding: 40px;
            border-radius: 20px;
            position: relative;
            transition: all 0.3s;
            box-shadow: 0 5px 20px rgba(0, 0, 0, 0.05);
        }

        .testimonial-card::before {
            content: '"';
            position: absolute;
            top: 20px;
            left: 30px;
            font-size: 80px;
            color: var(--light-blue);
            font-family: Georgia, serif;
            line-height: 1;
        }

        .testimonial-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
        }

        .testimonial-text {
            font-size: 17px;
            line-height: 1.7;
            color: var(--text-dark);
            margin-bottom: 30px;
            position: relative;
            z-index: 1;
        }

        .testimonial-author {
            display: flex;
            align-items: center;
            gap: 16px;
        }

        .author-image {
            width: 60px;
            height: 60px;
            border-radius: 50%;
            background: linear-gradient(135deg, var(--primary-blue), var(--accent));
            flex-shrink: 0;
        }

        .author-info h4 {
            font-size: 18px;
            margin-bottom: 4px;
            color: var(--text-dark);
        }

        .author-info p {
            font-size: 14px;
            color: var(--text-gray);
        }

        /* About Section */
        .about {
            background: var(--white);
        }

        .about-content {
            display: grid;
            grid-template-columns: 1fr 2fr;
            gap: 60px;
            align-items: center;
            max-width: 900px;
            margin: 0 auto;
        }

        .about-image {
            text-align: center;
        }

        .portrait {
            width: 220px;
            height: 220px;
            border-radius: 50%;
            background: linear-gradient(135deg, var(--primary-blue), var(--accent));
            margin: 0 auto 20px;
            border: 6px solid var(--white);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
        }

        .about-text h3 {
            font-size: 32px;
            margin-bottom: 20px;
            color: var(--text-dark);
        }

        .about-text p {
            font-size: 18px;
            line-height: 1.8;
            color: var(--text-gray);
        }

        /* Final CTA Section */
        .final-cta {
            background: linear-gradient(135deg, var(--primary-blue), var(--dark-blue));
            color: var(--white);
            text-align: center;
            padding: 100px 0;
        }

        .final-cta h2 {
            font-size: 48px;
            margin-bottom: 24px;
            color: var(--white);
        }

        .final-cta p {
            font-size: 20px;
            margin-bottom: 40px;
            opacity: 0.95;
        }

        /* Footer */
        footer {
            background: var(--text-dark);
            color: var(--white);
            padding: 80px 0 30px;
        }

        .footer-content {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 60px;
            margin-bottom: 50px;
        }

        .footer-links {
            display: flex;
            gap: 30px;
            flex-wrap: wrap;
        }

        .footer-links a {
            color: var(--text-gray);
            text-decoration: none;
            transition: color 0.3s;
        }

        .footer-links a:hover {
            color: var(--white);
        }

        .contact-form {
            max-width: 500px;
        }

        .contact-form h4 {
            font-size: 24px;
            margin-bottom: 20px;
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-group input,
        .form-group textarea {
            width: 100%;
            padding: 14px;
            border: 1px solid #374151;
            background: #1f2937;
            color: var(--white);
            border-radius: 8px;
            font-family: inherit;
            font-size: 16px;
            transition: all 0.3s;
        }

        .form-group input:focus,
        .form-group textarea:focus {
            outline: none;
            border-color: var(--primary-blue);
            background: #252d3a;
        }

        .form-group textarea {
            resize: vertical;
            min-height: 120px;
        }

        .footer-bottom {
            text-align: center;
            padding-top: 30px;
            border-top: 1px solid #374151;
            color: var(--text-gray);
            font-size: 14px;
        }

        /* Scroll Animations */
        .scroll-animate {
            opacity: 0;
            transform: translateY(30px);
            transition: all 0.6s ease-out;
        }

        .scroll-animate.active {
            opacity: 1;
            transform: translateY(0);
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .nav-links {
                display: none;
            }

            .mobile-menu {
                display: flex;
            }

            .hero-content h1 {
                font-size: 40px;
            }

            .hero-content p {
                font-size: 18px;
            }

            .hero-buttons {
                flex-direction: column;
                align-items: stretch;
            }

            .section-header h2 {
                font-size: 32px;
            }

            .features-grid,
            .benefits-grid,
            .testimonials-grid,
            .images-grid {
                grid-template-columns: 1fr;
            }

            .about-content {
                grid-template-columns: 1fr;
                text-align: center;
            }

            .footer-content {
                grid-template-columns: 1fr;
            }

            .final-cta h2 {
                font-size: 36px;
            }
        }
    </style>
</head>
<body>
    <!-- Navigation -->
    <nav id="navbar">
        <div class="container">
            <div class="nav-content">
                <div class="logo" onclick="scrollToSection('hero')">Nexa CRM</div>
                <div class="nav-links">
                    <a href="#home" onclick="scrollToSection('hero')">Home</a>
                    <a href="#features" onclick="scrollToSection('features')">Features</a>
                    <a href="#pricing" onclick="scrollToSection('pricing')">Pricing</a>
                    <a href="#about" onclick="scrollToSection('about')">About</a>
                    <a href="#contact" onclick="scrollToSection('contact')">Contact</a>
                    <a href="#pricing" class="btn-nav" onclick="scrollToSection('pricing')">Start Free Trial</a>
                </div>
                <div class="mobile-menu">
                    <span></span>
                    <span></span>
                    <span></span>
                </div>
            </div>
        </div>
    </nav>

    <!-- Hero Section -->
    <section id="hero" class="hero">
        <div class="container">
            <div class="hero-content">
                <h1>Simplify Your Business Growth with Nexa CRM</h1>
                <p>Manage clients, automate marketing, and grow your sales — all in one place.</p>
                <div class="hero-buttons">
                    <a href="#pricing" class="btn-primary" onclick="scrollToSection('pricing')">Start Free Trial</a>
                    <a href="#contact" class="btn-secondary" onclick="scrollToSection('contact')">Book a Demo</a>
                </div>
            </div>
        </div>
    </section>

    <!-- Features Section -->
    <section id="features" class="features">
        <div class="container">
            <div class="section-header scroll-animate">
                <h2>Everything You Need to Succeed</h2>
                <p>Powerful features designed to streamline your business operations</p>
            </div>
            <div class="features-grid">
                <div class="feature-card scroll-animate">
                    <div class="feature-icon">👥</div>
                    <h3>Client & Lead Management</h3>
                    <p>Centralize all customer data and interactions in one intuitive platform for stronger relationships.</p>
                </div>
                <div class="feature-card scroll-animate">
                    <div class="feature-icon">📧</div>
                    <h3>Email & SMS Marketing</h3>
                    <p>Create automated campaigns that nurture leads and convert prospects into loyal customers.</p>
                </div>
                <div class="feature-card scroll-animate">
                    <div class="feature-icon">🎯</div>
                    <h3>Sales Funnels & Pipelines</h3>
                    <p>Build high-converting funnels and visualize your entire sales process from lead to close.</p>
                </div>
                <div class="feature-card scroll-animate">
                    <div class="feature-icon">📅</div>
                    <h3>Appointment Scheduling</h3>
                    <p>Integrated calendar system that eliminates scheduling friction and maximizes your time.</p>
                </div>
                <div class="feature-card scroll-animate">
                    <div class="feature-icon">⚡</div>
                    <h3>Automation Workflows</h3>
                    <p>Set up intelligent automations that handle repetitive tasks while you focus on growth.</p>
                </div>
                <div class="feature-card scroll-animate">
                    <div class="feature-icon">📊</div>
                    <h3>Analytics & Reporting</h3>
                    <p>Real-time insights and comprehensive reports to make data-driven business decisions.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Benefits Section -->
    <section id="benefits" class="benefits">
        <div class="container">
            <div class="section-header scroll-animate">
                <h2>Transform Your Business Operations</h2>
                <p>See the impact Nexa CRM can have on your business</p>
            </div>
            <div class="benefits-grid">
                <div class="benefit-card scroll-animate">
                    <div class="benefit-icon">⏰</div>
                    <h3>Save Time with Automation</h3>
                    <p>Automate repetitive tasks and reclaim hours every week to focus on what truly matters.</p>
                </div>
                <div class="benefit-card scroll-animate">
                    <div class="benefit-icon">💰</div>
                    <h3>Close More Deals Faster</h3>
                    <p>Optimize your sales process with intelligent pipelines and never miss a follow-up again.</p>
                </div>
                <div class="benefit-card scroll-animate">
                    <div class="benefit-icon">🤝</div>
                    <h3>Keep Every Client Relationship Organized</h3>
                    <p>Build stronger connections with complete visibility into every customer interaction.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Product Images Section -->
    <section id="product-images" class="product-images">
        <div class="container">
            <div class="section-header scroll-animate">
                <h2>See Nexa CRM in Detail</h2>
                <p>Explore the intuitive interface and powerful features</p>
            </div>
            <div class="images-grid">
                <div class="image-card scroll-animate">
                    <div class="image-placeholder">📊</div>
                    <div class="image-caption">
                        <h3>Powerful Dashboard</h3>
                        <p>Get a complete overview of your business with real-time analytics, KPIs, and performance metrics all in one place.</p>
                    </div>
                </div>
                <div class="image-card scroll-animate">
                    <div class="image-placeholder">👥</div>
                    <div class="image-caption">
                        <h3>Contact Management</h3>
                        <p>Organize all your contacts, track interactions, and view complete customer histories with our intuitive interface.</p>
                    </div>
                </div>
                <div class="image-card scroll-animate">
                    <div class="image-placeholder">📈</div>
                    <div class="image-caption">
                        <h3>Sales Pipeline View</h3>
                        <p>Visualize your entire sales pipeline with drag-and-drop functionality to manage deals through every stage.</p>
                    </div>
                </div>
                <div class="image-card scroll-animate">
                    <div class="image-placeholder">📧</div>
                    <div class="image-caption">
                        <h3>Email Campaign Builder</h3>
                        <p>Create beautiful, responsive email campaigns with our easy-to-use drag-and-drop editor and templates.</p>
                    </div>
                </div>
                <div class="image-card scroll-animate">
                    <div class="image-placeholder">⚙️</div>
                    <div class="image-caption">
                        <h3>Automation Workflows</h3>
                        <p>Design complex automation workflows visually with our intuitive workflow builder to save time and boost efficiency.</p>
                    </div>
                </div>
                <div class="image-card scroll-animate">
                    <div class="image-placeholder">📅</div>
                    <div class="image-caption">
                        <h3>Calendar & Scheduling</h3>
                        <p>Manage appointments, set reminders, and sync with your favorite calendar apps for seamless scheduling.</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Video Demo Section -->
    <section id="video-demo" class="video-demo">
        <div class="container">
            <div class="section-header scroll-animate">
                <h2>See Nexa CRM in Action</h2>
                <p>Watch how easy it is to manage clients, automate marketing, and boost sales</p>
            </div>
            <div class="video-container scroll-animate">
                <div class="video-placeholder" onclick="playVideo()">
                    <div class="play-button">▶</div>
                </div>
            </div>
        </div>
    </section>

    <!-- Pricing Section -->
    <section id="pricing" class="pricing">
        <div class="container">
            <div class="section-header scroll-animate">
                <h2>Simple, Transparent Pricing</h2>
                <p>Everything you need to grow, in one affordable package</p>
            </div>
            <div class="pricing-container">
                <div class="pricing-card scroll-animate">
                    <div class="pricing-header">
                        <h3>Nexa CRM Subscription</h3>
                        <div class="pricing-price">$79<span>/month</span></div>
                        <p>Full CRM access for 1 business account</p>
                    </div>
                    <ul class="pricing-features">
                        <li>Contact & lead management</li>
                        <li>Email & SMS campaigns</li>
                        <li>Sales pipeline & funnel builder</li>
                        <li>Appointment booking</li>
                        <li>Automation workflows</li>
                        <li>Analytics & reporting</li>
                        <li>Priority customer support</li>
                        <li>Free onboarding & training</li>
                    </ul>
                    <a href="#contact" class="btn-primary" style="width: 100%; text-align: center;" onclick="scrollToSection('contact')">Get Started</a>
                </div>
            </div>
        </div>
    </section>

    <!-- Testimonials Section -->
    <section id="testimonials" class="testimonials">
        <div class="container">
            <div class="section-header scroll-animate">
                <h2>Loved by Businesses Worldwide</h2>
                <p>See what our customers have to say</p>
            </div>
            <div class="testimonials-grid">
                <div class="testimonial-card scroll-animate">
                    <div class="testimonial-text">
                        Nexa CRM completely transformed how we manage our client relationships. The automation features alone have saved us 15 hours a week. Our conversion rates have doubled since switching.
                    </div>
                    <div class="testimonial-author">
                        <div class="author-image"></div>
                        <div class="author-info">
                            <h4>Sarah Mitchell</h4>
                            <p>CEO, TechFlow Solutions</p>
                        </div>
                    </div>
                </div>
                <div class="testimonial-card scroll-animate">
                    <div class="testimonial-text">
                        The pipeline visualization and automated follow-ups are game-changers. We've closed 40% more deals since implementing Nexa CRM. It's like having an extra sales team member.
                    </div>
                    <div class="testimonial-author">
                        <div class="author-image"></div>
                        <div class="author-info">
                            <h4>Marcus Chen</h4>
                            <p>Sales Director, GrowthLabs</p>
                        </div>
                    </div>
                </div>
                <div class="testimonial-card scroll-animate">
                    <div class="testimonial-text">
                        Finally, a CRM that's actually intuitive to use. Our entire team was onboarded in days, not weeks. The analytics dashboard gives us insights we never had before.
                    </div>
                    <div class="testimonial-author">
                        <div class="author-image"></div>
                        <div class="author-info">
                            <h4>Emily Rodriguez</h4>
                            <p>Operations Manager, BlueSky Consulting</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- About Me Section -->
    <section id="about" class="about">
        <div class="container">
            <div class="section-header scroll-animate">
                <h2>Meet the Founder</h2>
            </div>
            <div class="about-content">
                <div class="about-image scroll-animate">
                    <div class="portrait"></div>
                </div>
                <div class="about-text scroll-animate">
                    <h3>Hi, I'm Najeeb</h3>
                    <p>
                        A Software Quality Assurance Engineer and entrepreneur passionate about helping businesses simplify their operations. I created Nexa CRM to give business owners the tools they need to automate, scale, and grow. My mission is to empower entrepreneurs with technology that makes business management effortless, so you can focus on what you do best—serving your customers and growing your vision.
                    </p>
                </div>
            </div>
        </div>
    </section>

    <!-- Final CTA Section -->
    <section class="final-cta">
        <div class="container">
            <h2>Ready to Simplify Your Business Growth?</h2>
            <p>Join thousands of businesses that trust Nexa CRM to manage their growth</p>
            <div class="hero-buttons">
                <a href="#pricing" class="btn-primary" style="background: white; color: var(--primary-blue);" onclick="scrollToSection('pricing')">Start Free Trial</a>
                <a href="#contact" class="btn-secondary" style="border-color: white; color: white;" onclick="scrollToSection('contact')">Book a Demo</a>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer id="contact">
        <div class="container">
            <div class="footer-content">
                <div class="footer-info">
                    <div class="logo" style="font-size: 32px; margin-bottom: 20px;">Nexa CRM</div>
                    <p style="color: var(--text-gray); margin-bottom: 30px;">Empowering businesses with intelligent automation and seamless client management solutions.</p>
                    <div class="footer-links">
                        <a href="#home" onclick="scrollToSection('hero')">Home</a>
                        <a href="#features" onclick="scrollToSection('features')">Features</a>
                        <a href="#pricing" onclick="scrollToSection('pricing')">Pricing</a>
                        <a href="#about" onclick="scrollToSection('about')">About</a>
                        <a href="#contact" onclick="scrollToSection('contact')">Contact</a>
                    </div>
                </div>
                <div class="contact-form">
                    <h4>Get in Touch</h4>
                    <form onsubmit="handleSubmit(event)">
                        <div class="form-group">
                            <input type="text" placeholder="Your Name" required>
                        </div>
                        <div class="form-group">
                            <input type="email" placeholder="Your Email" required>
                        </div>
                        <div class="form-group">
                            <textarea placeholder="Your Message" required></textarea>
                        </div>
                        <button type="submit" class="btn-primary" style="width: 100%;">Send Message</button>
                    </form>
                </div>
            </div>
            <div class="footer-bottom">
                <p>&copy; 2025 Nexa CRM. All rights reserved. | Made with ❤️ for businesses that want to grow</p>
            </div>
        </div>
    </footer>

    <script>
        // Smooth scroll navigation
        function scrollToSection(sectionId) {
            event.preventDefault();
            const section = document.getElementById(sectionId);
            if (section) {
                section.scrollIntoView({ behavior: 'smooth' });
            }
        }

        // Navbar scroll effect
        window.addEventListener('scroll', function() {
            const navbar = document.getElementById('navbar');
            if (window.scrollY > 50) {
                navbar.classList.add('scrolled');
            } else {
                navbar.classList.remove('scrolled');
            }
        });

        // Scroll animation observer
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -50px 0px'
        };

        const observer = new IntersectionObserver(function(entries) {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('active');
                }
            });
        }, observerOptions);

        document.addEventListener('DOMContentLoaded', function() {
            const animatedElements = document.querySelectorAll('.scroll-animate');
            animatedElements.forEach(el => observer.observe(el));
        });

        // Form submission
        function handleSubmit(event) {
            event.preventDefault();
            alert('Thank you for your message! We\'ll get back to you soon.');
            event.target.reset();
        }

        // Video play simulation
        function playVideo() {
            alert('Video demo would play here. In production, this would load your actual demo video (YouTube, Vimeo, or self-hosted).');
        }

        // Mobile menu toggle (basic implementation)
        document.querySelector('.mobile-menu')?.addEventListener('click', function() {
            alert('Mobile menu - In production, this would toggle a mobile navigation menu');
        });
    </script>
</body>
</html>
